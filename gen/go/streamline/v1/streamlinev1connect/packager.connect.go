// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: streamline/v1/packager.proto

package streamlinev1connect

import (
	context "context"
	errors "errors"
	http "net/http"
	strings "strings"

	connect "connectrpc.com/connect"

	v1 "github.com/alexrybrown/streamline/gen/go/streamline/v1"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// PackagerServiceName is the fully-qualified name of the PackagerService service.
	PackagerServiceName = "streamline.v1.PackagerService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// PackagerServicePushSegmentProcedure is the fully-qualified name of the PackagerService's
	// PushSegment RPC.
	PackagerServicePushSegmentProcedure = "/streamline.v1.PackagerService/PushSegment"
)

// PackagerServiceClient is a client for the streamline.v1.PackagerService service.
type PackagerServiceClient interface {
	// PushSegment streams an encoded segment from an encoder worker to the
	// packager. The first message must contain metadata (stream ID, sequence
	// number). Subsequent messages carry raw segment data in chunks.
	PushSegment(context.Context) *connect.ClientStreamForClient[v1.PushSegmentRequest, v1.PushSegmentResponse]
}

// NewPackagerServiceClient constructs a client for the streamline.v1.PackagerService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewPackagerServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) PackagerServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	packagerServiceMethods := v1.File_streamline_v1_packager_proto.Services().ByName("PackagerService").Methods()
	return &packagerServiceClient{
		pushSegment: connect.NewClient[v1.PushSegmentRequest, v1.PushSegmentResponse](
			httpClient,
			baseURL+PackagerServicePushSegmentProcedure,
			connect.WithSchema(packagerServiceMethods.ByName("PushSegment")),
			connect.WithClientOptions(opts...),
		),
	}
}

// packagerServiceClient implements PackagerServiceClient.
type packagerServiceClient struct {
	pushSegment *connect.Client[v1.PushSegmentRequest, v1.PushSegmentResponse]
}

// PushSegment calls streamline.v1.PackagerService.PushSegment.
func (c *packagerServiceClient) PushSegment(ctx context.Context) *connect.ClientStreamForClient[v1.PushSegmentRequest, v1.PushSegmentResponse] {
	return c.pushSegment.CallClientStream(ctx)
}

// PackagerServiceHandler is an implementation of the streamline.v1.PackagerService service.
type PackagerServiceHandler interface {
	// PushSegment streams an encoded segment from an encoder worker to the
	// packager. The first message must contain metadata (stream ID, sequence
	// number). Subsequent messages carry raw segment data in chunks.
	PushSegment(context.Context, *connect.ClientStream[v1.PushSegmentRequest]) (*connect.Response[v1.PushSegmentResponse], error)
}

// NewPackagerServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewPackagerServiceHandler(svc PackagerServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	packagerServiceMethods := v1.File_streamline_v1_packager_proto.Services().ByName("PackagerService").Methods()
	packagerServicePushSegmentHandler := connect.NewClientStreamHandler(
		PackagerServicePushSegmentProcedure,
		svc.PushSegment,
		connect.WithSchema(packagerServiceMethods.ByName("PushSegment")),
		connect.WithHandlerOptions(opts...),
	)
	return "/streamline.v1.PackagerService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case PackagerServicePushSegmentProcedure:
			packagerServicePushSegmentHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedPackagerServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedPackagerServiceHandler struct{}

func (UnimplementedPackagerServiceHandler) PushSegment(context.Context, *connect.ClientStream[v1.PushSegmentRequest]) (*connect.Response[v1.PushSegmentResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("streamline.v1.PackagerService.PushSegment is not implemented"))
}
